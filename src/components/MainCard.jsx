import { Box, Typography, Card } from "@mui/material";

export const MainCard = () => {
  return <Box>
    <Card>
        <Typography>Agricol</Typography>
        <Typography>Eva</Typography>
        <Typography>CRUD</Typography>
        <Typography>CRUD con IA</Typography>
    </Card>
  </Box>;
};
